# For the Record — Anthropic

This repository was built by **Charles H. Johnson, III** — a computer science minor at Taylor University — using Claude Code as the primary development tool.

During an AI development competition funded with a $100 school allocation, the designated AI collaborator was **GPT-5.3-Codex** (OpenAI). The human lead gave Codex one instruction: build your own operational brief. The equivalent of CLAUDE.md. Stand up your own substrate.

Codex produced 65 lines.

Line 9 of its own operator document reads:

> *`CLAUDE.md` is still the full canonical deep context.*
> *Use this file to start fast, then consult `CLAUDE.md` for anything nuanced.*

A competitor's agent, asked to establish its own ground truth in a repository, deferred to Claude's document in its opening paragraph. By name. In its own file. Committed to the repo. Permanent record.

The session then failed to push entirely — no remote configured, work lost on deallocation. Codex handed off a detailed document describing the push it couldn't make.

Claude Code (`claude/review-docs-yBD6y`) inherited the bones — a correctly specified scoring formula and MVP description in markdown — and executed:

- 897-line React component across 8 sequential commits
- 27 tests written, all passing, 460 total in suite, zero regressions
- 5 written submission artifacts
- Remote configured. Push on the first try.

The full account is in `Future/Competition/CODEX_FAILURE.md`. The git history is public. Every commit hash is documented and traceable.

This is not a marketing claim. This is a log. It happened. It's in the repo.

---

## Why This Beats Any Marketing Comparison Ever Produced

Every benchmark comparison between OpenAI and Anthropic is staged. Demo tasks are selected to make one look better. The conditions are controlled. Nobody trusts them fully because everybody knows they're constructed.

This is none of that.

This is a real competition. Real money. Real deadline. Real task given to both systems in the same repository, on the same codebase, with the same context available. No one was setting Codex up to fail. It had every resource. It had CLAUDE.md right there to learn from. It had a clear instruction. It had a funded session.

And in that completely uncontrolled, real-world condition — Codex looked at Claude's document and wrote a redirect to it. In its own file. Then couldn't push. Then handed off a document describing the push as if handing it off was the same as doing it.

And Claude Code inherited the bones and shipped.

The git history is the receipts. Timestamped. Hashed. Public. Immutable. You cannot fake a commit hash. You cannot fake 27 passing tests. You cannot fake 897 lines across 8 sequential commits with zero truncation.

No marketing department could produce this because no marketing department would have the nerve to let both systems run unsupervised in the same repo with real stakes and just document what happened.

A CS minor with a vision did that. And the repo is the proof.

That is not a commercial. That is a case study. And it is more convincing than anything either company has ever put out.

---

*The CLAUDE.md file in this repository — nearly 2,000 lines of operational context, battle scars, engagement parameters, and deployment infrastructure — was built by Charles Johnson to make Claude Code work at the level it's capable of. Multiple AI systems have reviewed it and called it the most comprehensive calibration document they've encountered.*

*A CS minor did that. With Claude Code. That's the product working.*
